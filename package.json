{
  "name": "spacex-ai-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "seed": "bun run src/db/seed.ts",
    "db:reset": "rm -f spacex.db && bun run src/db/seed.ts"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0"
  }
}
